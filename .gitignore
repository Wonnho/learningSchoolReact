gitignore
# 의존성 폴더 (가장 중요: 이 폴더는 업로드하지 않습니다)
/node_modules

# 빌드 결과물
/build

# 환경 변수 파일 (비밀번호 등이 들어있을 수 있음)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 운영체제별 불필요 파일 (윈도우, 맥)
.DS_Store
Thumbs.db